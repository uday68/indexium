apiVersion: apps/v1
kind: Deployment
metadata:
  name: indexium-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: indexium-redis
  template:
    metadata:
      labels:
        app: indexium-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  type: ClusterIP
  selector:
    app: indexium-redis
  ports:
    - port: 6379
      targetPort: 6379
